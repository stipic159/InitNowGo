// В будующем пригодится
/*
export interface SessionData {
  waitingForNumber?: boolean;
  pendingNumber?: {
    userId: number;
    callbackMessageId?: number;
  };
  lastCallbackTime?: number;
  lastRequestTime?: number;
  expectationCode?: boolean;
  pendingCode?: {
    userId: string;
    number: string;
    photoId?: string
  };
  resendRequests?: Array<{ userId: string; number: string }>;
  errorMessageIds?: number[];
  peddingForwarded?: {
    userId: string;
    message: any;
  }
}
*/